"""Node-cluster builders for the SpinSci switchboard graph.

Each module in this package builds one of the conversation-phase clusters
(Greeting, Business Hours, After Hours, Authentication, Routing) and downstream
segments (Scheduling) as a set of workflow graph nodes and edges using the
engine's existing primitives.
"""
